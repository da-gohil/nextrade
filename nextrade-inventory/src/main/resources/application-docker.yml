spring:
  datasource:
    url: jdbc:mysql://mysql:3306/inventory_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
  kafka:
    bootstrap-servers: kafka:9092
  data:
    redis:
      host: redis
eureka:
  client:
    service-url:
      defaultZone: http://discovery:8761/eureka
