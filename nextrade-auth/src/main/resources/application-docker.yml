spring:
  datasource:
    url: jdbc:mysql://mysql:3306/auth_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

eureka:
  client:
    service-url:
      defaultZone: http://discovery:8761/eureka
